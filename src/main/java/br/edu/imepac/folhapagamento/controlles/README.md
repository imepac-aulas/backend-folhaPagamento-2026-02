# controlles

Pacote que reúne os controladores (controllers) REST da aplicação. Expõe os endpoints HTTP utilizados pelo front-end, recebendo as requisições, validando a entrada e delegando o processamento para a camada de services.
